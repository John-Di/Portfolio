import React, {
  useContext
} from "react";
import CartPage from "../../layouts/cart";


// const ProductContext = React.createContext(DUMMY_PRODUCT);

// markup
const Cart = () => {
  return (
    <CartPage />
  )
}

export default Cart;
