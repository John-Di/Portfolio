import styled from 'styled-components';
import { size } from '../../utils/variables';
import { SECTION } from '../section/styles';

SECTION.hasPadding = false;
export const FULL_WIDTH_SECTION = SECTION;
